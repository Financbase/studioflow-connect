
// Re-export the toast hooks and functions from the main implementation
import { useToast, toast, ToastFunction } from "@/hooks/use-toast";

export { useToast, toast };
export type { ToastFunction };
