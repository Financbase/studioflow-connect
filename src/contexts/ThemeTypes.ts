
export type ThemeVariant = "modern" | "legacy" | "classic" | "windows" | "default" | "retro";
export type ThemeMode = "dark" | "light";

// Re-export the ColorPalette type from ColorPaletteContext for convenience
export { type ColorPalette } from "./ColorPaletteContext";
